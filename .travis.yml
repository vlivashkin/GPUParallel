language: python
python:
- 3.7
before_install:
  - python --version
  - pip install -U pip
install:
- pip install .
script: python -m unittest discover
after_success:
  - bash <(curl -s https://codecov.io/bash)
